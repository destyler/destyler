import { describe, expect, it } from 'vitest'
import { mount } from '@vue/test-utils'
import { } from '../src'

describe('basic', () => {
  it('should be true', () => {
    expect(true).toBe(true)
  })
})
